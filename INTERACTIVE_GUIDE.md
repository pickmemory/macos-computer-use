# 🎯 Interactive AI Mode - 完整指南

## 核心理念

**强制 AI 真正看到截图，返回精准坐标！**

这是唯一正确的工作方式。之前的所有问题都源于 AI 没有真正看到截图就猜测坐标。

---

## 工作流程

```
┌──────────────────────────────────────────────────────────────────┐
│  1. 脚本截图                                                      │
│     → /tmp/macos-ai-session/step-01-xxx.png                      │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│  2. 你在聊天中使用 `image` 工具分析                                 │
│     image /tmp/.../step-01-xxx.png "下一步做什么？"               │
│     → AI 真正看到图片                                             │
│     → AI 返回精准坐标                                              │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│  3. 将 AI 返回的 JSON 指令粘贴到脚本                                  │
│     {"action": "click", "params": {"x": 1440, "y": 100}}        │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│  4. 脚本执行指令                                                  │
│     python3 mouse.py click 1440 100                              │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│  5. 验证（再次截图，AI 确认成功）                                    │
│     image /tmp/.../step-01-verify.png "操作成功了吗？"            │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│  6. 循环直到任务完成                                              │
└──────────────────────────────────────────────────────────────────┘
```

---

## 快速开始

### 步骤 1: 启动脚本

```bash
cd ~/.openclaw/workspace/skills/macos-computer-use
source .venv/bin/activate

python3 scripts/interactive_ai.py -t "在 Freeform 中画一只猫"
```

### 步骤 2: 按提示操作

脚本会显示：

```
======================================================================
📍 步骤 1/20
======================================================================

📸 截图已保存：/tmp/macos-ai-session/step-01-1772028000.png

======================================================================
🧠 AI 分析步骤
======================================================================

📋 任务：当前界面状态？下一步应该做什么？请返回 JSON 指令。

📸 截图：/tmp/macos-ai-session/step-01-1772028000.png

======================================================================
⚠️  请在聊天中发送以下内容（使用 image 工具，不是 read！）:
======================================================================

image /tmp/macos-ai-session/step-01-1772028000.png "当前界面状态？下一步应该做什么？请返回 JSON 指令。"

======================================================================
⏳ 等待 AI 分析并返回指令...
======================================================================

AI 应该返回类似：
{"action": "click", "params": {"x": 1440, "y": 100}, "reason": "点击画笔工具"}

请输入 AI 返回的指令 (JSON 格式，或 'q' 退出):
```

### 步骤 3: 在聊天中分析

**关键步骤！** 脚本会自动包含屏幕信息。在聊天中发送：

```
image /tmp/macos-ai-session/step-01-1772028000.png "当前界面状态？下一步应该做什么？请返回 JSON 指令。屏幕：2560 x 1600 Retina。请返回基于截图实际像素的坐标（Retina 2x）。"
```

⚠️ **重要：屏幕分辨率信息会自动包含在提示中！**

- MacBook Air Retina: **2560 x 1600**（逻辑）
- 实际截图：**5120 x 3200**（2x 缩放）
- AI 会知道按实际像素返回坐标

AI 会分析截图并返回：

```json
{
  "action": "hotkey",
  "params": {"keys": ["cmd", "n"]},
  "reason": "新建画布 (Cmd+N)"
}
```

### 步骤 4: 粘贴指令回脚本

在脚本提示处粘贴：

```
请输入 AI 返回的指令 (JSON 格式，或 'q' 退出): {"action": "hotkey", "params": {"keys": ["cmd", "n"]}, "reason": "新建画布"}

✅ 收到指令：hotkey

======================================================================
🤖 执行：hotkey
======================================================================
   ⌨️  快捷键：cmd+n
```

### 步骤 5: 验证

```
📸 截图已保存：/tmp/macos-ai-session/step-01-verify-1772028010.png

======================================================================
✅ 验证步骤
======================================================================

📸 截图：/tmp/macos-ai-session/step-01-verify-1772028010.png

======================================================================
⚠️  请在聊天中发送：
======================================================================

image /tmp/macos-ai-session/step-01-verify-1772028010.png "新建画布成功了吗？"

======================================================================

操作成功了吗？(y/n/q): y
✅ 验证通过，继续下一步
```

### 步骤 6: 循环继续

重复以上流程，直到任务完成。

---

## JSON 指令格式

### click - 点击
```json
{
  "action": "click",
  "params": {"x": 1440, "y": 100},
  "reason": "点击画笔工具"
}
```

### drag - 拖拽
```json
{
  "action": "drag",
  "params": {
    "from": [1400, 700],
    "to": [1600, 900],
    "duration": 0.8
  },
  "reason": "画猫头（圆形）"
}
```

### hotkey - 快捷键
```json
{
  "action": "hotkey",
  "params": {"keys": ["cmd", "n"]},
  "reason": "新建画布"
}
```

### type - 输入文字
```json
{
  "action": "type",
  "params": {"text": "Hello"},
  "reason": "输入文字"
}
```

### wait - 等待
```json
{
  "action": "wait",
  "params": {"seconds": 2},
  "reason": "等待应用加载"
}
```

### done - 完成任务
```json
{
  "action": "done",
  "params": {"message": "猫画好了！"},
  "reason": "任务完成"
}
```

---

## 完整示例：画一只猫

### 步骤 1: 新建画布
```
image /tmp/.../step-01.png "Freeform 界面，如何新建画布？"
→ {"action": "hotkey", "params": {"keys": ["cmd", "n"]}}
```

### 步骤 2: 选择画笔
```
image /tmp/.../step-02.png "画布打开了吗？如何选择画笔？"
→ {"action": "hotkey", "params": {"keys": ["cmd", "2"]}}
```

### 步骤 3: 画猫头
```
image /tmp/.../step-03.png "画笔选中了吗？如何画猫头（圆形）？坐标？"
→ {"action": "drag", "params": {"from": [1400, 700], "to": [1600, 900]}}
```

### 步骤 4: 画耳朵
```
image /tmp/.../step-04.png "猫头画好了吗？如何画左耳朵？坐标？"
→ {"action": "drag", "params": {"from": [1380, 680], "to": [1350, 600]}}
```

### 步骤 5-10: 继续画其他部分...

### 步骤 11: 完成
```
image /tmp/.../step-11.png "猫画好了吗？"
→ {"action": "done", "params": {"message": "任务完成！"}}
```

---

## Fail Fast 原则

**每步都必须验证！**

```
❌ 错误：
   画头 → 画耳朵 → 画眼睛 → ... → 最后验证（太晚了）

✅ 正确：
   画头 → 验证 ✅ → 画耳朵 → 验证 ✅ → ...
              ↓失败             ↓失败
           停止调整          停止调整
```

**为什么重要：**
- 如果画笔没选中，画什么都没用
- 如果坐标错了，继续只会更糟
- 每步验证，问题立即可见

---

## 常见问题

### Q: 为什么要这么麻烦？
**A**: 这是唯一能让 AI 真正看到截图的方式。之前的失败都是因为 AI 猜测坐标。

### Q: 不能自动分析吗？
**A**: 可以，但需要集成视觉 API。当前方式最简单可靠。

### Q: JSON 格式错了怎么办？
**A**: 脚本会提示重新输入，不会执行错误指令。

### Q: 可以中途退出吗？
**A**: 可以，输入 `q` 或按 Ctrl+C。

---

## 总结

**Interactive AI Mode 的核心：**

1. 📸 **脚本截图** - 自动保存
2. 🧠 **AI 分析** - 在聊天中用 `image` 工具（关键！）
3. 📋 **粘贴指令** - JSON 格式
4. 🤖 **执行** - 脚本自动执行
5. ✅ **验证** - 每步都确认成功
6. 🔄 **循环** - 直到完成

**这是正确的工作方式！** 🚀
