# 🖥️ 屏幕分辨率与坐标系统

## 关键信息

**MacBook Air Retina 屏幕：**
- **逻辑分辨率**: 2560 x 1600
- **缩放比例**: 2x (Retina)
- **实际像素**: 5120 x 3200

---

## 坐标系统说明

### 截图 vs 逻辑坐标

```
┌─────────────────────────────────────────┐
│  逻辑坐标 (系统显示)                     │
│  2560 x 1600                            │
│  ┌─────────────────┐                    │
│  │ 点击 (1000, 500) │ ← 系统坐标         │
│  └─────────────────┘                    │
└─────────────────────────────────────────┘
              ↓ 2x 缩放
┌─────────────────────────────────────────┐
│  实际像素 (截图)                         │
│  5120 x 3200                            │
│  ┌─────────────────────────────────┐    │
│  │ 点击 (2000, 1000) │ ← 实际像素   │    │
│  └─────────────────────────────────┘    │
└─────────────────────────────────────────┘
```

### 重要规则

**截图中的坐标 = 逻辑坐标 × 2**

```
系统说：点击 (1000, 500)
截图实际：点击 (2000, 1000)
```

---

## 为什么需要告诉 AI 屏幕信息

### 问题场景

**如果 AI 不知道 Retina 缩放：**

1. AI 看到截图（5120x3200）
2. AI 以为这是 1x 屏幕
3. AI 说"点击中央 (2560, 1600)"
4. 实际点击位置：屏幕的 1/4 处 ❌

**正确做法：**

1. 告诉 AI："这是 2560x1600 Retina，实际像素 5120x3200"
2. AI 知道按 2x 计算
3. AI 说"点击中央 (2560, 1600)" - 这是实际像素
4. 执行点击，位置正确 ✅

---

## interactive_ai.py 自动处理

脚本会自动检测并包含屏幕信息：

```
🖥️  屏幕：Resolution: 2560 x 1600 Retina
⚠️  坐标基于实际截图像素（Retina 2x 缩放）

请在聊天中发送：
image /tmp/.../step-01.png "... 屏幕：2560 x 1600 Retina。
请返回基于截图实际像素的坐标（Retina 2x）。"
```

---

## AI 返回的坐标格式

### 正确格式（实际像素）

```json
{
  "action": "click",
  "params": {"x": 2880, "y": 400},
  "reason": "点击顶部工具栏"
}
```

这个坐标是**截图的实际像素**，可以直接执行。

### 错误格式（逻辑坐标）

```json
{
  "action": "click",
  "params": {"x": 1440, "y": 200},
  "reason": "点击顶部工具栏"
}
```

这个坐标是**逻辑坐标**，需要 ×2 才能用。

---

## 验证坐标是否正确

### 方法 1: 截图尺寸检查

```bash
screencapture -x /tmp/test.png
file /tmp/test.png
# 输出：PNG image data, 5120 x 3200 ← 实际像素
```

### 方法 2: 测试点击

```bash
# 点击屏幕中央
python3 mouse.py click 2560 1600
# 应该点击到 5120x3200 截图的正中央
```

---

## 其他 Mac 型号参考

| 设备 | 逻辑分辨率 | 缩放 | 实际像素 |
|------|-----------|------|---------|
| MacBook Air 13" | 2560 x 1600 | 2x | 5120 x 3200 |
| MacBook Pro 14" | 3024 x 1964 | 2x | 6048 x 3928 |
| MacBook Pro 16" | 3456 x 2234 | 2x | 6912 x 4468 |
| iMac 24" | 4480 x 2520 | 2x | 8960 x 5040 |
| Pro Display XDR | 6016 x 3384 | 1x | 6016 x 3384 |

---

## 总结

**关键要点：**
1. ✅ Retina 屏幕截图是**实际像素**（2x）
2. ✅ AI 需要知道屏幕信息才能返回正确坐标
3. ✅ `interactive_ai.py` 自动检测并包含屏幕信息
4. ✅ 坐标必须基于**实际像素**，不是逻辑坐标

**永远记住：**
> 截图尺寸 = 逻辑分辨率 × 缩放比例
> 
> 坐标 = 截图中的实际像素位置

---

**现在 AI 知道你的屏幕配置了！** 🎯
